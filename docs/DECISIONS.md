# 山外山 决策日志（DECISIONS）

> **本文件是活文档，随任务推进持续迭代更新。** 不要视作最终定稿。
> 更新规则：
> - 新决策拍板 → 填"决策"并标 `[已确认 YYYY-MM-DD]`
> - 执行中发现需调整 → 改"决策"并加 `[已修订 YYYY-MM-DD]` 备注原因
> - 新出现的待决事项 → 追加到"待迭代/待验证"区
> - **优先级铁律：DECISIONS.md < API 文档(ZHIHU-API.md) < REFERENCE-PROJECTS.md < PRD(山外山PRD.md)。所有决策若与 PRD 冲突，一律以 PRD 为主。**
> 关联文件：`山外山PRD.md`（需求真相，最高优先级）、`ZHIHU-API.md`（API 真相）、`REFERENCE-PROJECTS.md`（参考项目真相）

---

## 一、技术选型（MVP）

### D-01 前端框架 `[已确认 2026-08-27，框架定为 React]`
- 候选：Vue / React
- **决策：使用 React + Vite（不再用原生 JS）。**
- 理由：用户授权 AI 自行选定（"选好弄不踩坑的"）。选 React 因：① PRD 模块5原话即 React，对齐无需改稿；② 参考项目 ArguMesh 同为 React，可借鉴组件；③ 社区最大、踩坑率最低。
- 注：PRD 第7节已据用户意图改为"前端框架（Vue 或 React）"，现据本决策细化为 React。

### D-02 后端框架 `[已确认 2026-08-27]`
- 候选：保留 Node 原生 http（server.mjs）/ 升级 Hono
- **决策：保留现有 Node 原生 http（server.mjs）**
- 理由：PRD 第7节写"知乎官方 API 经 Node 后端代理 + 文件缓存"，未要求 Hono；现有 server.mjs 已能跑。
- 待迭代：若需 SSE 流式（见 D-流式）或路由增多，再评估 Hono/Express。

### D-03 3D 工牌（Three.js 物理引擎） `[已确认 2026-08-27]`
- 候选：保留 / MVP 砍掉降级静态页
- **决策：MVP 砍掉。降级为静态刘看山引导页/图（无 3D 物理）**
- 理由：用户拍板"MVP 先不做 3D"。
- 待迭代：演示完成后有余力再作为加分项补回（PRD 模块5/创新点仍提及，但非 MVP）。

### D-04 PRD 技术架构矛盾统一 `[已确认 2026-08-27]`
- 矛盾：PRD 第4节模块5原写"React + Three.js"（服务于已砍的 3D 工牌），第7节原写"原生 JavaScript + HTML"（用户未改完的旧表述）。
- **决策：以 PRD 当前修订版为准——第7节已改为"前端框架（Vue 或 React）"，第4节模块5的 React 仅服务于被砍工牌。统一为"前端框架 + Node 后端代理"架构。**
- 备注：模块5 的 React 提法随 D-03 工牌被砍而失效，不再作为架构约束。

### D-08 框架对齐 ArguMesh 的取舍 `[已确认 2026-08-27]`
- 决策：选 React（D-01 已定），技术栈部分对齐 ArguMesh（React + Vite）。仅借鉴其"证据溯源/留白"方法论（见 REFERENCE-PROJECTS.md 项目8），不强制搬其 Hono/SQLite 后端。

---

## 二、产品范围（MVP 砍留）

### D-05 OAuth / 关注流分享链路 `[已确认 2026-08-27]`
- 候选：做 / 不做
- **决策：MVP 不做。**
- 理由：用户拍板"MVP 先不做"。且 ZHIHU-API.md 证实关注流/粉丝接口 CLI 不支持；OAuth 回调需公网 HTTPS，本地跑不通（PRD 第9节风险"高(确定)"）。
- 待迭代：作为演示可选增强写进"计划借鉴未做"（PRD 8.2(3)）。
> 2026-08-30：鉴权环境变量命名对齐 OpenAI，见 D-11

### D-06 "翻面看来源文章"实现方式 `[已确认 2026-08-27]`
- 要求（用户原话）："我需要的是你的所有论点论据都是有根据（来自于知乎），具体的实现手法无所谓。"
- **决策：每条论点/论据必须能溯源到具体知乎回答（来源链接/作者/出处）；实现手法不限。**
- 硬约束：对峙墙/推荐里出现的任何"最强论点""适合谁""边界"都必须标注知乎来源，禁止无出处编造。
- 事实限制：ZHIHU-API.md 证实知乎无论文级引用 API，只能靠搜索聚合"同一问题其他回答"变通。
- 待迭代：验证现有 `zhihu.js` 的 `topicMock` 兜底是否产生"无真实来源"假对峙（PRD 8.1(1) 隐患），MVP 演示需用 LIVE 或真实种子数据避免假对峙。

### D-07 简历解析方案 `[已确认 2026-08-27]`
- 候选：大模型提取 / 知识库 RAG
- **决策：先用现有"大模型提取结构化字段"跑通（PRD 第7节方案）；知识库接口(knowledge/upload+search)作为增强备选。**
- 理由：用户认可推荐方案。PRD 第7节已定义文本提取后调大模型输出受约束字段，原始文件解析后删除。
- 待迭代：若大模型提取不稳定，启用知识库 RAG 兜底。

### D-09 产品定位收束与旧版文档归档 `[已确认 2026-08-30]`
- 背景：产品于 08-27 从《知乎炼金术》（话题判断训练）转型为《山外山》（求职决策），三份转型前文档与现行定位不符。
- **决策：产品定位以《山外山PRD.md》为唯一真相源；`TEAM_INTRO.md` / `plan-material.md` / `references.md` 属转型前旧版，归档至 `docs/_archive/zhihu-alchemy-legacy/`，仅作背景素材，不再更新。**
- 接替关系：references.md → REFERENCE-PROJECTS.md；TEAM_INTRO 的产品叙事 → PRD（四大 UI 卡片名仍适用于 React 版）；TEAM_INTRO 技术上手信息 → 根 README。
- 样例简历 `sample-resume.md` 移至 `public/sample-resume.md`（前端 fetch 该路径，随 Vite 构建打包；此前放在 docs/ 导致 `/sample-resume.md` 404，属 docs 迁移引入的回归，本次一并修复）。

### D-10 简历解析链路收束 + 遗留代码清理 + 健康检查降耗 `[已确认 2026-08-30]`
- **简历解析链路**：浏览器端为主链路——PDF（pdfjs）/ DOCX（mammoth）/ 图片 OCR（tesseract.js 懒加载）/ TXT·MD 直读，部署**零 Python 依赖**；`/api/resume` 结构化抽取 **StepFun 优先、知乎直答兜底**（此前模块③进度记录只写"直答抽取"，以本条为准）。`md_server.py`（MarkItDown）定位为**后续拓展模块**：仅覆盖基础文字格式及 xls/ppt/doc 等冷门格式兜底，暂不部署，评委链路不依赖；未来扫描件 OCR 走 `markitdown-ocr` 插件 + 视觉 LLM 或前端"pdfjs 渲染页面 → tesseract"方案。
- **遗留代码清理**：删除旧版前端页面（`public/index.html` / `app.js` / `styles.css`）、`personas.js`（无引用，内容已在 `src/lib.js`）、`copy_badges.py`（服务于已砍的 3D 工牌）。`server.mjs` 静态服务只托管 `dist/`，未构建时明确 404 提示（消除"静默回退旧版页面"陷阱）。`public/` 仅保留 Vite 静态资源。`/api/hot` 热榜与 OAuth 骨架保留在后端；"热榜灵感"如需在 React 版首屏呈现，作为后续小任务。
- **健康检查降耗**：`/api/health` 从"跑一次完整生成"改为调用免费额度接口 `/api/v1/quota`（新增 `zhihu.js#zhihuQuota()`，5 分钟缓存），不再消耗直答 100 次/天 配额，并顺带把剩余额度展示给前端（落地原"配额保护"待办的前半部分）。

### D-11 鉴权与直答环境变量 OpenAI 兼容命名 `[已确认 2026-08-30]`
- 背景：知乎直答接口本身已是 OpenAI 兼容格式（`POST /v1/chat/completions` + `messages` + `choices[0].message.content`），但鉴权环境变量沿用赛事命名 `ZHIHU_ACCESS_SECRET`，在部署平台与 OpenAI 生态（网关/开发者习惯/通用工具链）中命名不兼容。
- **决策：直答侧命名对齐 OpenAI——鉴权主名 `OPENAI_API_KEY`，旧名 `ZHIHU_ACCESS_SECRET` 保留为回退（`process.env.OPENAI_API_KEY || process.env.ZHIHU_ACCESS_SECRET`）；直答端点参数化为 `OPENAI_BASE_URL`（默认 `https://developer.zhihu.com/v1`，调用 `{OPENAI_BASE_URL}/chat/completions`）、模型参数化为 `OPENAI_MODEL`（默认 `zhida-fast-1p5`），默认值下直答 URL 与模型与现状逐字节一致。热榜/搜索/额度等原生端点（`/api/v1/*`）不受影响，鉴权头（Bearer + X-Request-Timestamp）与缓存/生成逻辑均不变。**
- 影响：`docs/DEPLOY.md`（方式一/方式二与环境变量表）、`README.md` 用户文案已同步；`.env` 平滑迁移——旧名仍被回退识别、不迁移也不致断，改配新名即可（MarkItDown `md_server.py` 零改动）。

---

## 三、缓存策略冲突分析（PRD 第6节 vs ZHIHU-API.md）`[已确认 2026-08-27]`

**冲突点**：PRD 第6节（第145/147行）与 API 文档给的 TTL 不一致。

| 接口 | PRD 第6节要求 | ZHIHU-API.md 给的 | 是否冲突 |
|---|---|---|---|
| 搜索(zhihu_search/global_search) | 按"身份+关键词"缓存 **3 天** | TTL **1 小时**（按 query hash） | ⚠️ 冲突（PRD 更长） |
| 故事/回答 API | 按关键词缓存 **7 天** | 无独立"故事 API"；直答 agent_answer TTL 1 小时 | ⚠️ 部分冲突 |
| 热榜 hot_list | PRD 未明确 | TTL **4 小时**，每日拉一次 | — |
| 直答 chat/completions | PRD 未明确 | TTL **1 小时**（按 query hash） | — |

**决策：以 PRD 第6节为主（最大程度按 PRD 要求处理）。** 即搜索缓存 3 天、回答缓存 7 天。`[未实现 2026-08-30]`：代码实际仍为 API 文档层 TTL（搜索 1h / 直答 10min / 热榜 1h）。黑客松演示场景下收益有限，**缓议**；如需实现，按下述双层方案执行。

**为什么和 API 文档不同？能否解决？**
- 原因：API 文档的 TTL 是"知乎官方建议的防抖窗口"（避免短时间重复打接口）；PRD 的 3天/7天是"产品层业务缓存"（用户处境几天内不变，不必重拉）。两者目的不同，**不矛盾，可叠加**。
- 解决方式：实现上采用**双层缓存**——
  1. **业务缓存（PRD 为准）**：搜索结果按"身份+关键词"存 3 天、回答存 7 天，命中直接返回，不触接口；
  2. **接口层兜底（API 文档为准）**：即便业务缓存失效，调用前仍检查 API 文档的 1 小时/4 小时最小窗口，避免单品类每天超 1 次拉取、热榜超 100 次/天。
  - 这样既满足 PRD"缓存 3/7 天"的体验要求，又不突破 API 文档的官方配额与防抖建议。
- 备注：PRD 第6节写"单品类每天仅 1 次拉取最新内容"——与 API 文档"搜索 5000次/天"不冲突（1次/天是产品节流，远低于 5000 上限）。`zhihu.js` 现有简易文件缓存需补充"按身份+关键词"维度与 3/7 天 TTL。

---

## 四、已确认决策沉淀区（速查）

| 编号 | 决策 | 状态 |
|---|---|---|
| D-01 | 前端换框架，定为 **React+Vite**（不再原生 JS） | 已确认 2026-08-27 |
| D-02 | 后端保留 Node 原生 http | 已确认 2026-08-27 |
| D-03 | 3D 工牌 MVP 砍掉，降级静态页 | 已确认 2026-08-27 |
| D-04 | PRD 矛盾统一为"前端框架 + Node 后端" | 已确认 2026-08-27 |
| D-05 | OAuth/关注流 MVP 不做 | 已确认 2026-08-27 |
| D-06 | 论点必须溯源知乎，手法不限 | 已确认 2026-08-27 |
| D-07 | 简历先大模型提取，RAG 备选 | 已确认 2026-08-27 |
| D-08 | 框架对齐 ArguMesh：选 React 即部分对齐 | 已确认 2026-08-27 |
| D-09 | 定位以山外山 PRD 为准；旧版三文档归档 _archive | 已确认 2026-08-30 |
| D-10 | 简历解析浏览器端为主（MarkItDown 降后续拓展）；删旧版前端；health 走免费额度接口 | 已确认 2026-08-30 |
| D-11 | 直答侧命名对齐 OpenAI（OPENAI_API_KEY 主名+旧名回退；OPENAI_BASE_URL/OPENAI_MODEL 参数化） | 已确认 2026-08-30 |
| 缓存 | 以 PRD 3天/7天为主，双层缓存兼容 API 文档 | 已确认 2026-08-27；`[未实现 2026-08-30]` 缓议 |

---

## 五、MVP 进度（随任务推进更新）

- [x] **骨架搭建 2026-08-27**：React+Vite 前端工程建立；`server.mjs` 增加 POST `/api/alchemy` + 托管 `dist/`；4 大模块组件（Onboarding/ConflictWall/Quiz/ActionMap）从原 app.js 平移完成
- [x] **构建验证 2026-08-27**：`npm install` + `npm run build` 成功（dist 产物，含 pdfjs/mammoth）
- [x] **接口验证 2026-08-27**：live 模式真实调用 `/api/alchemy`，返回含真实知乎 sourceItems
- [x] **模块① 阶段/目标 2026-08-27**：7阶段单选 + 目标多选(含"暂未明确"独占) + 城市/时间/困惑输入，对齐 PRD 模块1
- [x] **模块② 处境卡 2026-08-27**：新增 PersonaCard 组件，生成可编辑处境卡（预览/编辑/确认），确认后才进推荐
- [x] **模块③ 简历解析 2026-08-27**：`/api/resume` 接口 + `extractResume`（直答抽取字段）；前端支持 PDF(pdfjs)/DOCX(mammoth)/TXT 上传 + 内置样例 `sample-resume.md`；解析失败降级手动填写。**实测：样例文本→抽取 education/experience/skills/industry/roles 成功**
- [x] **模块④ 检索+匹配 2026-08-27**：`alchemy` 用处境卡 queries 多次检索合并；prompt 注入处境卡上下文；每个角色输出 `matchReason`（匹配用户处境）。前端 ConflictWall 渲染"🎯 匹配你的处境"
- [x] **模块⑤ 行动清单 2026-08-27**：`alchemy` 接收处境卡生成 actions；前端 ActionMap 渲染+本地勾选持久化
- [x] **前端页面联调 2026-08-28**：localhost:3000 全流程跑通（建档→处境卡→简历→对峙墙→自测→行动→导出）；定位并修复"JSON 解析失败"根因（之前误用 `npm run preview` 纯前端，缺 `/api/*` 后端，改为 `npm run start` 完整服务）
- [x] **行业/细分自由填写 2026-08-28**：预设行业从 8 扩至 18 个；新增"自定义行业/领域"单输入框（不区分行业与细分，填写后覆盖预设；清空即恢复下拉）；预设两组下拉在自定义时显示"无"并置灰提示
- [x] **城市选择器重构 2026-08-28**：参照 BOSS 直聘交互，新增 CityPicker 抽屉——搜索框 + 热门/省会推荐 + 拼音首字母 A-Z 分组（200+ 城市）+ 右侧字母索引；搜索时隐藏索引并滚动置顶
- [x] **简历图片 OCR 2026-08-28**：`resume.js` 接入 tesseract.js（动态懒加载，首屏 JS 仅 16KB），支持 JPG/PNG/WebP/BMP 图片简历 OCR 识别文字；失败降级提示；同时新增"粘贴简历文字"入口兜底
- [x] **真实 API 接入 2026-08-28**：把赛事 Access Secret 写入 `.env`（`ZHIHU_ACCESS_SECRET`），后端 `/api/alchemy`、`/api/resume` 正式调真实知乎数据；新增 `/api/health` 健康检查接口 + 前端"🔌 测试连接"按钮，一键验证 secret 是否有效
- [x] **文档收束与链路清理 2026-08-30**：D-09/D-10 落地——旧版三文档归档 `_archive/`、根 README 按山外山定位重写、PRD 过时表述加注同步、简历解析改浏览器端主链路（MarkItDown 降后续拓展）、删除旧版前端与死代码、`/api/health` 改走免费额度接口、样例简历回归修复（移至 `public/`）
- [x] **细节打磨 2026-08-30**：dominantSide 已修复（自测立场改按角色 id 统计、派别名随当前对峙数据显示）；导出 Markdown 已有。剩余可选项：导出 MD 格式微调、"热榜灵感"React 版入口、剩余额度常态化提示

## 六、待迭代 / 待验证

- [x] **D-01 具体框架**：已定为 React（用户授权 AI 自选）
- [x] **⚠️ 直答限流 554 复测（2026-08-30 实测，工单 09 命名改造后）**：554 限流**未复现**，quota 接口正常（`Code:0 success`）。但直答 chat/completions 首次调用 25s 超时（`[zhida] error TimeoutError`，`zhihu.js` AbortSignal.timeout(25000) 触发），系统按设计回落 mock 三派（`fallback:true`，来源仍为真实搜索结果）；第 2 次调用成功——真实议题派别（稳航派/疾风派/平衡舵）+ 个性化 matchReason + 真实可点击来源。**结论：通道可用但响应慢且偶发超时，25s 上限偏紧 → 待开票：调大直答超时（如 60s）/超时重试一次。**额度现实：当前 Secret 为试用额度——直答 Total **2**、热榜 Total 2、搜索 Total 10、知识库 500、用户数据 1000（与文档「直答/热榜 100 次/天」严重不符；两次调用后计数器仍 used:0，计费口径未知）；**演示日必须换赛事专用 Secret**（获取途径见 `.env.example` 注释：赛事群发放比赛专用 Secret + token 补贴）。原始证据：`.scratch/shanwaishan-mvp/handoffs/assets/06-quota-raw.json`、`06-alchemy-raw{,-2}.json`（本地不入库）。

> `[已修订 2026-09-01]`：**上述额度事实已过时，「必须换赛事 Secret」的结论作废。** 9 月 1 日实测当前 Secret 额度为——直答 Total 100（已用 2、剩余 98）、热榜 Total 100（剩余 100）、知乎搜索 Total 5000（已用 2）、全网搜 5000、知识库 500、用户数据 10000、小工具 10。即**当前 Secret 已是赛事专用额度，无需更换**。原「直答 Total 2 / 热榜 2 / 搜索 10」为 2026-08-30 使用另一 Secret 时的观测值，此处仅作历史留痕，不再作为决策依据。修正依据见 `docs/PROGRESS-2026-09-01.md` 第四节。
- [x] **验证"假对峙"风险（2026-08-30 实测）**：两种形态均实测——① 直答超时回落：角色为 MOCK 通用三派（工程派/架构派/业务派），`fallback:true` 标记正确、来源替换为真实搜索结果（D-06 溯源底线在兜底形态下仍满足）；② 直答成功：真实行业派别 + 个性化 matchReason + 真实来源，假对峙不出现。**遗留缺口**：兜底路径（`!raw` 分支）的 mock 三派未补 matchReason（PRD 9.1 要求 100% matchReason）→ 需开票补齐；直答超时概率不低，演示日保持「自动回落演示数据」话术（设计行为非事故）。
- [ ] **缓存实现**：zhihu.js 补充"按身份+关键词"维度和 3/7 天 TTL（PRD 3/7天 + API 文档 1h/4h 兜底）
- [ ] **流式渲染**（PRD 8.2(1)）：体验需要时再评估，涉及 D-02 是否升 Hono
- [ ] **内容审核**：PRD 第7节提接入知乎内容审核规则，MVP 是否落地待定
- [x] **配额保护**：~~建议接入 `/api/v1/quota`~~ → **已全部落地 2026-08-30**：`/api/health` 走免费额度接口 + 「剩余额度常态化提示」（工单 05 QuotaHint：挂载自动拉取、直答剩余 <20 醒目预警、失败/无 Secret 整体隐藏）。注意：试用 Secret 直答 Total=2 必然常显预警——属真实稀缺提示非 bug；换赛事 Secret（100/天）后预警语义恢复正常。
- [x] **全网搜接入 2026-09-01**：`zhihu.js` 新增 `zhihuGlobalSearch`（GET `/api/v1/content/global_search`，5000/天，零依赖），`alchemy()` 检索阶段站内+全网双路并发、合并去重；corpus 与返回 `sources` 均做"站内+全网"双视角混合，补语料单一来源短板。新增 `scripts/global-search-check.mjs` 回归（8 项全过），既有 `scripts/fallback-check.mjs`（16 项）不受影响。
- [ ] **3D 工牌**：D-03 砍掉，演示完成后有余力再补（静态刘看山引导页替代）

- [ ] **额度利用率盘点与接口拓用** `[新增 2026-09-01]`：当前 Secret 额度大量闲置，几乎只用了直答一个接口。

  **实测额度（2026-09-01，`/api/health` 查询，该接口免费不消耗配额）：**

  | 接口 | 总额 | 已用 | 剩余 | 当前使用情况 |
  |---|---|---|---|---|
  | 直答（zhida_openai） | 100 | 2 | 98 | 在用（对峙墙/自测/行动地图生成） |
  | 知乎搜索（zhihu_search） | 5000 | 2 | 4998 | 在用（alchemy 语料检索） |
  | 全网搜（global_search） | 5000 | 0 | 5000 | **未用** |
  | 热榜（hot_list） | 100 | 0 | 100 | **未用**（入口已按产品定位移除） |
  | 知乎用户数据（user_data） | 10000 | 0 | 10000 | **未用**（需认证，当前不可用） |
  | 知识库（knowledge） | 500 | 0 | 500 | **未用**（需认证，当前不可用） |
  | 小工具（tools） | 10 | 0 | 10 | **未用**（API 文档未收录，能力待确认） |

  **关键区分：不是所有接口都是"不想用"，有一部分当前用不了。**

  A. **现在就能接（无需用户授权，低成本）**
  - **全网搜（5000/天）** `[已接入 2026-09-01]`：可补充知乎站外视角，解决当前语料"只有知乎站内"的单一来源问题；也可作为直答失败时的第二语料池。优先级最高、改动最小。**实现**：`zhihu.js#zhihuGlobalSearch` 新增（GET `/api/v1/content/global_search`，站外结果带 `source:'web'` 标记、无 URL 时兜底为知乎搜索链接）；`alchemy()` 检索阶段站内搜索与全局搜索**并发**调用、合并去重；corpus（站内前4 + 全网前4）与返回 `sources`（站内3 + 全网3）均做双视角混合，前端"来源区"直观体现全网搜已接入；无 Secret 时返回空（不影响 DEMO）。新增 `scripts/global-search-check.mjs` 回归（8 项断言全过）。
  - **热榜（100/天）**：注意——此前已按产品定位删除"热榜"入口（与求职无关）。若要复用，必须做成**行业/求职相关热榜**，不能退回泛娱乐热榜，否则与产品定位冲突。

  B. **需 OAuth 用户授权，当前被卡住**
  - **用户数据（10000/天，最高价值）**：关注列表 / 收藏夹 / 我的创作。对「信谁框架」价值最大——可用来判断答主可信度（认证信息、历史创作）、构建用户真实兴趣画像，把"按处境卡匹配"升级为"按真实关注圈匹配"（PRD 8.2(3) 的设想）。**前提是完成公网部署**（OAuth 回调需公网 HTTPS，D-05 已决策 MVP 不做）。
  - **知识库（500/天）**：可做简历解析 RAG 兜底（D-07 备选方案），或把知乎语料入库形成长期记忆。**同样依赖认证**。

  C. **待确认**
  - **小工具（10/天）**：`ZHIHU-API.md` 未收录该接口，能力范围未知，需向赛事方或平台文档确认后再评估。

  **建议优先级**：先接 A 组的全网搜（额度量大、零依赖、能补语料单一短板）；B 组与"公网部署 + OAuth"绑定，部署后再评估；C 组待确认。
  注：充分使用开放平台能力通常也是赛事评分的加分维度，值得在演示中体现。

---

## 七、修订历史

- 2026-08-30 **工单 06 LIVE 复测结论回填**：554 复测（未复现，改现 TimeoutError 偶发）、假对峙双形态验证、配额保护三项勾结；试用额度事实与赛事 Secret 待换事项记入待迭代区。

- 2026-08-27 创建初版（D-01~D-08 + 缓存）
- 2026-08-27 **修订**：全局规则改为"与 PRD 冲突一律以 PRD 为主"；D-01 改为换框架；PRD 第7节据意图改为框架
- 2026-08-27 **修订**：D-01 定为 React；完成 MVP 骨架搭建+构建+接口验证，新增第五、六、七节记录进度（活文档持续迭代）
- 2026-08-30 **新增 D-09**：定位收束为山外山 PRD，旧版三文档归档 `_archive/`；缓存决策标注未实现缓议
- 2026-08-30 **新增 D-10**：简历解析浏览器端为主（MarkItDown 降后续拓展）；删除旧版前端与死代码；health 改走免费额度接口
- 2026-08-30 **新增 D-11**：鉴权与直答环境变量 OpenAI 兼容命名（OPENAI_API_KEY 主名 + 旧名 ZHIHU_ACCESS_SECRET 回退；OPENAI_BASE_URL/OPENAI_MODEL 参数化直答端点与模型，默认值行为不变）
